"use client";
import {useState} from "react";
import {ConsentGate} from "../../components/ConsentGate";
import type {SourceSearchResponse} from "../../lib/api-types";
const query="title:(Backend Engineer) AND skills:(Python OR SQL OR AWS) AND location:India";
export default function Source(){const [data,setData]=useState<SourceSearchResponse>();const [error,setError]=useState("");async function search(){setError("");const response=await fetch(`${process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000"}/source/search`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({query})});if(!response.ok){setError("Search is unavailable. Check the backend connection.");return}setData(await response.json())}return <main><p className="eyebrow">Source · Q2</p><h1>Search people, then verify consent before outreach.</h1><div className="panel"><h2>Generated provider query</h2><code>{query}</code><p className="lede">The query is visible so an operator can audit the sourcing criteria before using it.</p><button onClick={search}>Search people</button>{error&&<p className="bad">{error}</p>}</div><ConsentGate/>{data&&<section className="panel"><p className="lede"><b>{data.source === "fixtures" ? "PDL fixtures — local demonstration data" : "PDL live search"}.</b> {data.reason}</p><table><thead><tr><th>Person</th><th>Role & location</th><th>Matched skills</th><th>Phone available</th><th>Action</th></tr></thead><tbody>{data.people.map(p=><tr key={p.id}><td>{p.name}<br/><span className="lede">{p.company}</span></td><td>{p.title}<br/><span className="lede">{p.location}</span></td><td>{p.skills.join(", ")}</td><td className={p.phone_available?'ok':'warn'}>{String(p.phone_available)}</td><td><button>Verify & record consent</button></td></tr>)}</tbody></table></section>}</main>}
