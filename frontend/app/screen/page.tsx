"use client";
import {useState} from "react"; import {ConsentGate} from "../../components/ConsentGate"; import {Funnel} from "../../components/Funnel";
const initial="We need a backend engineer with Python, SQL, Docker and AWS experience. The role is based in Bengaluru.";
export default function Screen(){const [jd,setJd]=useState(initial);const [schema,setSchema]=useState<Record<string,string>|null>(null);const [message,setMessage]=useState(""); async function derive(){const response=await fetch(`${process.env.NEXT_PUBLIC_API_URL??"http://localhost:8000"}/jobs/screen`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({jd})});if(!response.ok){setMessage("Could not derive the review schema. Check that the backend is running.");return}setSchema((await response.json()).result_schema);setMessage("Review the extraction below before provisioning an agent.")}return <main><p className="eyebrow">Screen · Q1</p><h1>Turn a job description into a reviewable voice screen.</h1><p className="lede">The operator approves the criteria and prompt before any candidate is contacted.</p><section className="panel"><label htmlFor="jd">Job description</label><textarea id="jd" rows={9} value={jd} onChange={e=>setJd(e.target.value)}/><button onClick={derive}>Derive screening schema</button>{message&&<p className="status ok">{message}</p>}</section>{schema&&<section className="panel"><h2>Approval boundary: result schema</h2><table><tbody>{Object.entries(schema).map(([key,value])=><tr key={key}><th>{key}</th><td>{value}</td></tr>)}</tbody></table><label>Language</label><select><option>English</option><option>Hindi</option><option>Tamil</option></select><button>Approve and provision agent</button></section>}<ConsentGate/><Funnel/></main>}
