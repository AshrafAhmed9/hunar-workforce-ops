# Attendance without smartphones

The complete Q3 design is maintained in the repository at `docs/attendance.md`. It proposes missed-call plus callback attendance, a supervisor reconciliation path, cost/throughput reasoning, anti-proxy controls, and firm boundaries on what an LLM may decide.
