[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
# FastAPI's dependency injection intentionally uses Depends() in parameter defaults.
ignore = ["B008"]
